
# User Space

User space is the part of the operating system where user applications run. It is separated from the kernel space, which is where the kernel runs. This separation is important for security and stability. If a user application crashes, it will not affect the kernel or other user applications.
